/** Default browser root font size, in pixels. */
export const DEFAULT_BASE_PX = 16;

/** Common pixel steps for the PX -> REM reference table. */
export const PX_STEPS = [1, 2, 4, 8, 12, 16, 20, 24, 32, 40, 48, 64];

/** Common rem steps for the REM -> PX reference table. */
export const REM_STEPS = [0.1, 0.25, 0.5, 0.75, 1, 1.5, 2, 2.5, 3, 4];
