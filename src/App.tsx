import SwaggerMinifier from './components/SwaggerMinifier';

const App = () => {
  return (
    <main className="min-h-screen p-4 lg:p-8">
      <SwaggerMinifier className="mx-auto max-w-7xl" />
    </main>
  );
};

export default App;
